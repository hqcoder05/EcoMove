server:
  port: ${PORT:8080}
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require&preferQueryMode=simple
    username: postgres.vlmlsyrwuyeqjjozvnbt
    password: 12345678
  hikari:
    data-source-properties:
      prepareThreshold: 0
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
          jdbc:
            time_zone: UTC
      database-platform: org.hibernate.dialect.PostgreSQLDialect
  jwt:
    secret: "XGSmokiuzGw0KSLSuPYCPICGkL3DsomcgLs+9c8qMhnUt+K/1soPJqvg8KyF3e/4"
    issuer: GiaoThongTM
    access-token-expiration-time: 3600
    refresh-token-expiration-time: 604800

ecomove:
  osrm:
    base-url: "https://router.project-osrm.org"   # hoặc http://your-osrm:5000
    profile: "driving"                             # driving / foot / bike
    table:
      max-destinations: 90                         # batch size (demo OSRM nên < 100)

